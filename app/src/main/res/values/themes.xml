<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SimpleWalaPanchwan" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="fontBoldStyle">
        <item name="fontFamily">@font/worksans</item>
        <item name="font">@font/bold</item>
    </style>
    <style name="Theme.SimpleWalaPanchwan" parent="Base.Theme.SimpleWalaPanchwan" />

    <style name="ProgressBarTheme" parent="ThemeOverlay.AppCompat.Light">
        <item name="colorAccent">@color/primary</item>
    </style>
</resources>